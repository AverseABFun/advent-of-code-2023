"use strict";
// No peeking!






























































































































































































































const fs = require('fs');

fs.readFile('../input/day2', 'utf8', (err, data) => {
    if (err) {
        console.error(err);
        return;
    }
    let splitData = data.split("\n");
});